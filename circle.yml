machine:
  node:
    version: 6.1.0

dependencies:
  override:
    - npm install
    - bower install

deployment:
  branch:
    branch: /^master$/
    commands:
      - rm -rf build
      - npm run build
      - rsync -avz --delete build/* deploy@andrewovens.com:/var/www/andrewovenscom/