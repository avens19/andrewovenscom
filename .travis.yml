language: node_js
node_js:
- lts/*
install:
   - npm install
   - ./node_modules/.bin/bower install
before_deploy:
   - rm -rf dist
   - npm run build
deploy:
  skip_cleanup: true
  provider: script
  script: scripts/deploy.sh
  on:
    branch: master
